/**
 * Bid Session UI plugin, node half. The browser half renders Host projection
 * values; this host entry exists only so the deployment can list the client
 * plugin in its Cordis composition.
 */

/** Host plugin body; Bid workflow behavior belongs to the Host control plane. */
export function apply(): void {}
